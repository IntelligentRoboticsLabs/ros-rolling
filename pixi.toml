[workspace]
name = "ros-rolling"
description = "RoboStack repo to package ros-rolling packages as conda packages"
authors = ["Tobias Fischer <tobias.fischer@qut.edu.au>", "Wolf Vollprecht <w.vollprecht@gmail.com>", "Silvio Traversaro <silvio@traversaro.it>", "Daisuke Nishimatsu <daisuke.nishimatsu1021@gmail.com>"]
channels = ["https://repo.prefix.dev/conda-forge"]
platforms = ["osx-arm64", "linux-64", "osx-64", "linux-aarch64", "win-64"]

[system-requirements]
# 2.17 is the glibc version used in centos 7
libc = { family="glibc", version="2.17" }

[dependencies]
python = ">=3.14.0,<3.15"
# Include the latest rattler-build fixes w.r.t. to patches handling
# rattler-build regression, pinning to 0.57
rattler-build = "<0.58.0"
cmake = "<4.0"
setuptools = "<82.0"
rattler-index = ">=0.27.16"

[target.win-64.dependencies]
# patch is required by rattler-build
m2-patch = "*"
# git is required by rattler-build
git = "*"

[pypi-dependencies]
# This is typically the latest commit on main branch
vinca = { git = "https://github.com/RoboStack/vinca.git", rev = "187a794e7dbdf51758213da65c33739f0d745f33" }
# Uncomment this line to work with a local vinca for faster iteration, but remember to comment it back
# (and regenerate the pixi.lock) once you push the modified commit to the repo
# vinca = { path = "../vinca", editable = true }

[tasks]
generate-recipes = { cmd = "vinca -m", depends-on = ["remove-recipes"] }
generate-gha-workflows = { cmd = "vinca-gha --trigger-branch dummy_build_branch_as_it_is_unused -d ./recipes", depends-on = ["generate-recipes"] }
check-patches = { cmd = "python check_patches_clean_apply.py", depends-on = ["generate-recipes"] }
create_snapshot = { cmd = "vinca-snapshot -d rolling -o rosdistro_snapshot.yaml" }
upload = "rattler-build upload prefix -c robostack-rolling --generate-attestation"
build_continue_on_failure = { cmd = "rattler-build build --recipe-dir ./recipes -m ./conda_build_config.yaml -c robostack-rolling -c https://repo.prefix.dev/conda-forge --continue-on-failure --skip-existing", depends-on = ["generate-recipes"] }

# Subset helpers (EasyNav + easynav_plugins)
# These tasks use ./easynav_subset/vinca.yaml to generate only the EasyNav-related recipes.

[tasks.easynav-clean]
cmd = "rm -rf output/{{ platform }} output/bld"
depends-on = ["remove-recipes"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Remove generated recipes and build artifacts for the EasyNav subset (recipes/, output/<platform>/, output/bld)."

[tasks.easynav-generate-recipes]
cmd = "vinca -d ./easynav_subset --platform {{ platform }} -m"
depends-on = ["easynav-clean"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Generate recipes for the EasyNav subset using easynav_subset/vinca.yaml."

[tasks.easynav-build]
cmd = "rattler-build build --recipe-dir ./recipes --target-platform {{ platform }} -m ./conda_build_config.yaml -c https://prefix.dev/robostack-rolling -c https://prefix.dev/conda-forge --skip-existing"
depends-on = ["easynav-generate-recipes"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Build conda packages for the EasyNav subset recipes."

[tasks.easynav-index]
cmd = "rattler-index fs ./output --force"
depends-on = ["easynav-build"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Index ./output as a local conda channel (repodata.json) after building."

[tasks.easynav-all]
cmd = "echo 'EasyNav subset: clean → recipes → build → index done.'"
depends-on = ["easynav-index"]
args = [{ arg = "platform", default = "linux-64" }]
description = "One-liner: clean, generate recipes, build, and index the EasyNav subset."

# Subset helpers (PlanSys2)
# These tasks use ./plansys2_subset/vinca.yaml to generate only the PlanSys2-related recipes.

[tasks.plansys2-clean]
cmd = "rm -rf output/{{ platform }} output/bld"
depends-on = ["remove-recipes"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Remove generated recipes and build artifacts for the PlanSys2 subset (recipes/, output/<platform>/, output/bld)."

[tasks.plansys2-generate-recipes]
cmd = "vinca -d ./plansys2_subset --platform {{ platform }} -m"
depends-on = ["plansys2-clean"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Generate recipes for the PlanSys2 subset using plansys2_subset/vinca.yaml."

[tasks.plansys2-build]
cmd = "rattler-build build --recipe-dir ./recipes --target-platform {{ platform }} -m ./conda_build_config.yaml -c https://prefix.dev/robostack-rolling -c https://prefix.dev/conda-forge --skip-existing"
depends-on = ["plansys2-build-popf", "plansys2-generate-recipes"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Build conda packages for the PlanSys2 subset recipes."

[tasks.plansys2-build-popf]
cmd = "bash -lc 'rattler-build build --recipe ./recipes/ros-rolling-popf/recipe.yaml --target-platform {{ platform }} -m ./conda_build_config.yaml -c https://prefix.dev/robostack-rolling -c https://prefix.dev/conda-forge --skip-existing && rattler-index fs ./output --force'"
depends-on = ["plansys2-generate-recipes"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Build ros-rolling-popf first (needed by plansys2-popf-plan-solver), so subsequent PlanSys2 recipes can resolve dependencies after a clean."

[tasks.plansys2-index]
cmd = "rattler-index fs ./output --force"
depends-on = ["plansys2-build"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Index ./output as a local conda channel (repodata.json) after building."

[tasks.plansys2-all]
cmd = "echo 'PlanSys2 subset: clean → recipes → build → index done.'"
depends-on = ["plansys2-index"]
args = [{ arg = "platform", default = "linux-64" }]
description = "One-liner: clean, generate recipes, build, and index the PlanSys2 subset."

# Subset helpers (NavMap)
# These tasks use ./navmap_subset/vinca.yaml to generate only the NavMap-related recipes.

[tasks.navmap-clean]
cmd = "rm -rf output/{{ platform }} output/bld"
depends-on = ["remove-recipes"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Remove generated recipes and build artifacts for the NavMap subset (recipes/, output/<platform>/, output/bld)."

[tasks.navmap-generate-recipes]
cmd = "vinca -d ./navmap_subset --platform {{ platform }} -m"
depends-on = ["navmap-clean"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Generate recipes for the NavMap subset using navmap_subset/vinca.yaml."

[tasks.navmap-build]
cmd = "rattler-build build --recipe-dir ./recipes --target-platform {{ platform }} -m ./conda_build_config.yaml -c https://prefix.dev/robostack-rolling -c https://prefix.dev/conda-forge --skip-existing"
depends-on = ["navmap-generate-recipes"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Build conda packages for the NavMap subset recipes."

[tasks.navmap-index]
cmd = "rattler-index fs ./output --force"
depends-on = ["navmap-build"]
args = [{ arg = "platform", default = "linux-64" }]
description = "Index ./output as a local conda channel (repodata.json) after building."

[tasks.navmap-all]
cmd = "echo 'NavMap subset: clean → recipes → build → index done.'"
depends-on = ["navmap-index"]
args = [{ arg = "platform", default = "linux-64" }]
description = "One-liner: clean, generate recipes, build, and index the NavMap subset."

[tasks.build]
cmd = "rattler-build build --recipe-dir ./recipes -m ./conda_build_config.yaml -c https://prefix.dev/robostack-rolling -c https://prefix.dev/conda-forge --skip-existing"
depends-on = ["generate-recipes"]
description = "Build all packages, from the ./recipes dir. This will skip already existing packages, so it can be used to build only a subset of packages by first removing the recipes of the packages you want to rebuild (see `pixi remove-recipes`)."

[tasks.remove-recipes]
cmd = "rm -rf recipes_only_patch; rm -rf recipes; mkdir recipes"
description = "Remove all generated recipes, before regenerating them."

[tasks.build-one]
cmd = "cp ./patch/{{ PACKAGE }}.*patch ./recipes/{{ PACKAGE }}/patch/; rattler-build build --recipe ./recipes/{{ PACKAGE }}/recipe.yaml -m ./conda_build_config.yaml -c https://prefix.dev/robostack-rolling -c https://prefix.dev/conda-forge"
args = [{ arg = "PACKAGE", default = "ros-rolling-ros-workspace" }]
description = "Build a single package, from the ./recipes dir. Add the `ros-rolling-` prefix to the package name, e.g. `pixi build-one --package ros-rolling-ros-workspace`"

